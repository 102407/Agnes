#define _CRT_SECURE_NO_WARNINGS 1
#include <stdio.h>
void tables(int x)
{
	int y = 0;
	for (y = 1; y <= x; y++)
	{
		printf("%2d*%2d=%-3d",x,y,x*y);
	}
}
int main()
{
	int a = 0;
	int b = 0;
	scanf("%d",&a);
	for (b = 1; b <= a; b++)
	{
		tables(b);//打印一行
		printf("\n");
	}
	return 0;
}
